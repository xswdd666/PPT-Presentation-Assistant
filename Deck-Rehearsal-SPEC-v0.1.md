# 汇报预演室（Deck Rehearsal）功能规格

> 版本：SPEC v0.1  
> 日期：2026-09-12  
> 状态：可实施评审稿  
> 需求基线：`Deck-Rehearsal-PRD-v0.2.md`  
> 当前仓库状态：纯产品文档，尚无应用代码、ADR、领域词汇表或既有测试  
> 发布状态：未发布到 issue tracker；当前项目未提供 issue tracker 和 `ready-for-agent` 标签配置

## Problem Statement

已经完成或接近完成 PPT 的用户，在重要汇报前通常缺少快速、稳定且贴近真实听众的预审反馈。用户过于熟悉自己的内容，难以发现陌生听众会在哪些页面看不懂、质疑证据、忘记重点或感到叙事跳跃；请同事评审又依赖他人时间，直接使用通用 AI 则需要自行组织 Prompt，反馈容易泛化、无法稳定定位到具体页面，也难以形成修改、复查和导出的完整闭环。

该问题同时存在于工作汇报、学生答辩、学术报告、创业路演和公开演讲。不同场景的评价标准不同，但共同需求是：围绕真实听众、汇报目的、内容证据、页面结构和现场表达，对用户已有 PPT 进行可靠的页面级调优，而不是重新生成一套材料。

用户还需要一种低风险的 AI 修改方式。AI 不应直接重写整页或覆盖原稿；用户应能够在页面中局部选中文字，让 AI 结合当前页与上下页的语义进行改写，清楚查看差异，再决定接受或不接受。任何被接受的改写在用户提交前都只是草稿，正式提交后应形成新版本并进行布局检查。

评审结果需要避免长篇报告和角色表演。不同模拟点评人的意见应通过右侧类似 Twitter/X 的评论流呈现：先显示头像、身份和短评论，用户按需展开完整依据。产品不提供独立的文字问答预演流程。

## Solution

汇报预演室允许用户上传最多 60 页的 PPTX，并填写汇报主题、场景、真实听众和预计时长。汇报目标与期望听众回应为可选字段；用户留空时，系统根据 PPT 内容、场景和听众生成可编辑的建议值，并明确区分“AI 建议”与“用户确认”。

系统确定性解析 PPTX 的文字、备注、元素位置、页面顺序和隐藏状态，将每页渲染为图片进行视觉理解，再生成整套演示文稿的共享事实底稿。主分析模型识别主要风险维度，默认邀请 3 位、最多 4 位职责不同的 AI 模拟点评人。评论正文限制为 50—100 个显示字符，问题与依据定位到具体页面，相关评论在右侧信息流中以头像、摘要和可展开详情呈现。

用户可以从评论定位到页面，在 PPT 页面内局部选择文字。选区出现明确但轻量的动画反馈和 AI 修改入口。AI 改写只针对选中内容，并读取当前页、上一页和下一页的文本、页面作用及视觉摘要，以保持前后衔接。结果以原文/建议 Diff 展示；接受后进入编辑草稿，不接受则恢复原文且不产生版本变化。

用户还可以修改标题、正文和备注，调整页面顺序，隐藏或恢复页面。提交修改会创建不可覆盖的新 PPT 版本，重新渲染受影响页面，检测文字溢出、遮挡和明显布局变化，并增量复查关联问题。用户完成主要修改后，可以基于当前版本生成逐页讲稿、衔接语、时间分配和潜在现场问题，并下载修改后的 PPTX。

首版在数据结构、场景选项、分析量表、角色路由、测试样本和讲稿风格上同时考虑以下五类用户：职场用户、学生、研究者、创业者和公开表达者。职场资源申请仅作为默认 Demo 场景，不作为系统能力边界。

## User Stories

### 项目与场景设置

1. 作为汇报用户，我想创建一个新的预演项目，以便把某次汇报的文件、背景、评论和版本集中管理。
2. 作为汇报用户，我想从项目列表看到项目名称、场景、更新时间、当前版本和处理进度，以便快速继续未完成的工作。
3. 作为汇报用户，我想删除不再需要的项目，以便控制自己的文件和数据。
4. 作为职场用户，我想选择工作汇报、项目汇报、述职或资源申请场景，以便系统采用管理和决策导向的评价标准。
5. 作为学生，我想选择课程展示或毕业答辩场景，以便系统关注结构完整、概念解释、论据和评委可能关注的问题。
6. 作为研究者，我想选择学术报告或研究汇报场景，以便系统关注研究问题、方法、证据、限制和引用。
7. 作为创业者，我想选择创业路演场景，以便系统关注痛点、市场、方案、验证、商业模式和诉求。
8. 作为公开表达者，我想选择公开演讲或方案展示场景，以便系统关注听众理解、记忆点、节奏和故事表达。
9. 作为无法匹配预设场景的用户，我想选择“其他”并描述场景，以便仍能使用评审功能。
10. 作为汇报用户，我想填写真实听众是谁，以便评论基于实际听众而不是泛化假设。
11. 作为汇报用户，我想填写预计汇报时长，以便系统判断内容密度和超时风险。
12. 作为汇报用户，我想补充自己最担心的问题，以便系统优先关注高焦虑、高风险内容。

### PPTX 上传与校验

13. 作为汇报用户，我想上传一套 PPTX，以便系统直接理解并修改原生演示文稿。
14. 作为汇报用户，我想上传最多 60 页的 PPTX，以便中长篇答辩、学术报告和路演也能被完整分析。
15. 作为汇报用户，我想在上传超过 60 页的文件时看到明确限制，以便知道如何处理文件后重试。
16. 作为汇报用户，我想在上传非 PPTX 文件时看到当前支持范围，以便不会误以为文件正在分析。
17. 作为汇报用户，我想在文件损坏、加密或无法解析时看到具体错误类型，以便采取针对性措施。
18. 作为汇报用户，我想看到上传进度和结果，以便确认大文件是否已成功传输。
19. 作为汇报用户，我想保留未经修改的原始文件，以便任何时候都能追溯初始材料。
20. 作为注重隐私的用户，我想在上传前知道文件会被如何解析和交给 AI 处理，以便做出知情决定。

### AI 语义补全汇报目标

21. 作为尚未想清楚目标的用户，我想把“汇报目标”留空，以便先上传材料而不被表单阻断。
22. 作为尚未想清楚听众回应的用户，我想把“期望听众回应”留空，以便系统根据内容帮助我提炼。
23. 作为汇报用户，我想让 AI 根据 PPT、场景和听众生成汇报目标建议，以便发现材料真正指向的目标。
24. 作为汇报用户，我想让 AI 生成期望听众回应建议，以便明确希望对方理解、认可、批准、选择或采取什么行动。
25. 作为汇报用户，我想清楚看到目标是“AI 建议”还是“用户确认”，以便理解该信息的可信等级。
26. 作为汇报用户，我想修改 AI 生成的目标和回应，以便让系统掌握真实意图。
27. 作为汇报用户，我想确认 AI 建议，以便后续角色路由、严重度排序和讲稿围绕已确认目标工作。
28. 作为汇报用户，我想在分析后继续修改目标，以便项目背景变化时可以重新校准评论。
29. 作为汇报用户，我想在目标变化后只重跑受影响的评审和排序，以便无需重新解析整套 PPT。

### 分析过程

30. 作为汇报用户，我想看到系统正在解析文件、理解视觉、分析结构、选择角色还是生成评论，以便理解等待状态。
31. 作为汇报用户，我想看到已处理页数，以便估计 60 页长文稿的分析进度。
32. 作为汇报用户，我想离开分析页面后再返回，以便无需停留等待。
33. 作为汇报用户，我想在单页处理失败时继续分析其余页面，以便局部异常不会使整套任务报废。
34. 作为汇报用户，我想看到发生异常的具体页码，以便检查该页内容。
35. 作为汇报用户，我想在某个分析阶段失败后重试，以便恢复暂时性错误。
36. 作为汇报用户，我想让系统直接读取 PPTX 原生文字而非再次 OCR，以便减少错字并加快处理。
37. 作为汇报用户，我想让系统理解图表、图片、阅读顺序和视觉层级，以便评论不仅依赖文本。
38. 作为汇报用户，我想让系统理解每页在整套汇报中的作用，以便发现跨页逻辑问题。
39. 作为汇报用户，我想让系统识别观点、证据、行动请求、重复和矛盾，以便得到整套叙事层面的反馈。

### 点评人路由

40. 作为汇报用户，我想让系统默认邀请最相关的 3 位点评人，以便快速获得互补视角。
41. 作为汇报用户，我想在存在第 4 个独立高风险维度时看到第 4 位点评人，以便重要问题不会被人数限制遗漏。
42. 作为汇报用户，我想知道每位点评人为什么被选中，以便判断角色是否与当前材料相关。
43. 作为汇报用户，我想看到路由理由关联的页面，以便验证系统不是只根据场景标签选人。
44. 作为汇报用户，我想取消不相关的点评人，以便控制评论噪声。
45. 作为汇报用户，我想邀请其他预设点评人，以便关注自己在意的专项问题。
46. 作为汇报用户，我想让新加入的点评人读取当前 PPT 版本和共享事实，以便评论不会基于过时内容。
47. 作为汇报用户，我想看到点评人明确标记为 AI 模拟听众，以便不会误认为背后是真人专家。
48. 作为汇报用户，我想让不同点评人遵守各自评价量表，以便多角色具有真实的信息增量。

### 右侧评论流

49. 作为汇报用户，我想在工作台右侧看到类似 Twitter/X 的评论流，以便一边看 PPT，一边浏览反馈。
50. 作为汇报用户，我想在点评人加入或评论生成时看到头像淡入、轻微缩放或状态点亮，以便感知新内容出现。
51. 作为容易被动画干扰的用户，我想让系统尊重“减少动态效果”设置，以便动画不会影响阅读和操作。
52. 作为汇报用户，我想在评论收起时看到头像、姓名、小标题、短评论、页码和生成状态，以便快速扫描。
53. 作为汇报用户，我想让每条评论正文保持在 50—100 个显示字符，以便信息流简洁可读。
54. 作为汇报用户，我想点击评论后在原位置展开完整详情，以便无需离开当前页面。
55. 作为汇报用户，我想在展开态看到判断依据、影响说明、建议动作和关联页面，以便评估建议是否可靠。
56. 作为汇报用户，我想从评论直接定位到对应页面和元素，以便快速查看上下文。
57. 作为汇报用户，我想按全部、未处理、高优先级和点评人筛选评论，以便专注当前任务。
58. 作为汇报用户，我想看到同一点评人的评论按生成顺序排列，以便理解其评审脉络。
59. 作为汇报用户，我想让相同根因的跨角色评论被合并为一个问题主题，以便避免多人重复说同一件事。
60. 作为汇报用户，我想在合并主题中看到每个角色不同的影响解释，以便保留多视角价值。
61. 作为汇报用户，我想默认只看到 5 个最高优先级问题主题，以便先处理最重要的问题。
62. 作为汇报用户，我想展开“更多问题”，以便在有时间时查看完整结果。
63. 作为汇报用户，我想把评论关联的问题加入修改清单，以便集中处理。
64. 作为汇报用户，我想接受可执行建议，以便记录该问题已进入修改流程。
65. 作为汇报用户，我想忽略不适用的建议并可选填写原因，以便保留决策记录并改进质量评估。
66. 作为窄屏设备用户，我想让评论面板切换为覆盖式抽屉，以便页面预览仍可使用。
67. 作为汇报用户，我不想被强制进入独立文字问答流程，以便评论浏览和材料修改保持轻量。

### 页面定位与局部文字选择

68. 作为汇报用户，我想从左侧缩略图看到页码、问题数和隐藏状态，以便快速导航整套 PPT。
69. 作为汇报用户，我想缩放当前页面并切换上下页，以便准确阅读和选择文字。
70. 作为汇报用户，我想在页面预览中拖动选择标题或正文中的连续文字，以便只修改需要优化的部分。
71. 作为汇报用户，我想看到选中文字的稳定高亮，以便确认系统识别了正确选区。
72. 作为汇报用户，我想在完成选择时看到轻量呼吸或聚焦动画，以便获得清晰的交互反馈。
73. 作为汇报用户，我想在选区附近看到“AI 修改”浮动入口，以便不必切换到其他编辑页面。
74. 作为汇报用户，我想取消选区，以便放弃误选内容。
75. 作为汇报用户，我想在 AI 处理中保持选区可见，以便知道当前修改作用于哪里。
76. 作为汇报用户，我想在 AI 处理中看到加载动画并阻止重复提交，以便避免生成多个冲突结果。
77. 作为汇报用户，我想在文本不能可靠映射回 PPT 元素时看到不可编辑提示，以便不会误以为修改可以安全保存。

### AI 局部改写

78. 作为汇报用户，我想让 AI 只改写选中的文字，以便保留未选内容和原始版式。
79. 作为汇报用户，我想让 AI 读取当前页内容，以便改写符合本页论点和视觉结构。
80. 作为汇报用户，我想让 AI 同时读取上一页内容，以便避免重复并保持承接自然。
81. 作为汇报用户，我想让 AI 同时读取下一页内容，以便改写能为后续页面做好铺垫。
82. 作为首页用户，我想让 AI 在没有上一页时正常工作，以便边界页面不会报错。
83. 作为末页用户，我想让 AI 在没有下一页时正常工作，以便边界页面不会报错。
84. 作为汇报用户，我想让 AI 保持原始数字、事实、术语和观点不变，以便语言优化不会产生事实漂移。
85. 作为汇报用户，我想让 AI 在上下文不足时说明缺少什么，而不是猜测，以便避免虚构内容。
86. 作为汇报用户，我想看到原文与 AI 建议的行内或浮层 Diff，以便理解具体变化。
87. 作为汇报用户，我想看到增加、删除和替换内容的明显区分，以便快速判断是否合适。
88. 作为汇报用户，我想接受 AI 修改，以便把建议加入当前编辑草稿。
89. 作为汇报用户，我想选择“不接受”，以便恢复原文且不产生版本变化。
90. 作为汇报用户，我想在接受后继续手工编辑建议文本，以便形成最终表达。
91. 作为汇报用户，我想在提交前撤销已接受的草稿修改，以便反悔而不影响历史版本。
92. 作为汇报用户，我想让系统记录 AI 建议、用户接受和最终提交之间的区别，以便分析建议质量。

### 受控编辑与页面结构

93. 作为汇报用户，我想修改现有标题，以便让页面结论更明确。
94. 作为汇报用户，我想修改现有正文文本框，以便精简或澄清内容。
95. 作为汇报用户，我想修改演讲者备注，以便同步口头表达准备。
96. 作为汇报用户，我想拖动页面调整顺序，以便修复叙事关系。
97. 作为汇报用户，我想隐藏页面而不是删除页面，以便将其作为备用材料保留。
98. 作为汇报用户，我想恢复隐藏页面，以便重新纳入主汇报。
99. 作为汇报用户，我想查看调整前后的结构差异，以便确认顺序变化符合预期。
100.  作为汇报用户，我想看到每页在背景、问题、证据、方案、结果或行动请求中的作用，以便理解整套叙事。
101.  作为汇报用户，我不想在产品中自由拖拽或缩放 PPT 元素，以便首版编辑行为保持可控和稳定。
102.  作为汇报用户，我不想让系统自动编辑图片、图表数据、SmartArt、动画、转场或母版，以便降低文件损坏风险。

### 版本、复查与导出

103. 作为汇报用户，我想在提交一组修改时创建新版本，以便历史文件永远不被覆盖。
104. 作为汇报用户，我想看到版本号、时间和修改摘要，以便知道每版发生了什么。
105. 作为汇报用户，我想查看历史版本，以便比较修改结果。
106. 作为汇报用户，我想恢复到历史版本，并把恢复操作记录为新版本，以便时间线保持完整。
107. 作为汇报用户，我想在修改文字后重新渲染受影响页面，以便看到真实排版结果。
108. 作为汇报用户，我想让系统检测文字溢出，以便避免下载后才发现内容放不下。
109. 作为汇报用户，我想让系统检测明显遮挡和布局变化，以便及时人工修复。
110. 作为汇报用户，我想在布局存在风险时看到页码级警告，以便知道应在 PowerPoint 中检查哪里。
111. 作为汇报用户，我想在局部文字变化后只复查关联页面和问题，以便降低等待时间。
112. 作为汇报用户，我想在页面排序后重新分析叙事、衔接和时间，以便全局建议保持最新。
113. 作为汇报用户，我想在隐藏或恢复页面后更新叙事和时长，以便结果反映当前演示路径。
114. 作为汇报用户，我想看到旧问题变为已解决、待复查、仍然存在或已失效，以便跟踪修改效果。
115. 作为汇报用户，我想下载当前版本 PPTX，以便回到 PowerPoint 完成最终调整和正式汇报。
116. 作为汇报用户，我想在存在布局警告时仍可下载，以便我能自行决定是否继续，但系统必须明确提醒风险。

### 逐页讲稿与时间准备

117. 作为汇报用户，我想基于当前 PPT 版本生成逐页讲稿，以便讲述内容与最终页面一致。
118. 作为汇报用户，我想看到每页的目的和必须说清楚的信息，以便知道讲述重点。
119. 作为汇报用户，我想得到推荐讲述顺序，以便避免照着页面机械朗读。
120. 作为汇报用户，我想得到上一页到当前页以及当前页到下一页的衔接语，以便切换自然。
121. 作为汇报用户，我想看到每页建议用时，以便控制整体节奏。
122. 作为汇报用户，我想在总建议时间超过目标时看到压缩建议，以便优先删减低价值内容。
123. 作为汇报用户，我想知道哪些内容可以省略或留到现场提问时再说，以便减少超时。
124. 作为汇报用户，我想看到每页可能引出的现场问题，以便提前准备但不进入独立问答流程。
125. 作为不同场景用户，我想选择自然口语、正式汇报、学生答辩或学术报告风格，以便讲稿符合场合。
126. 作为汇报用户，我想让讲稿不引入 PPT 和项目背景中不存在的新事实，以便避免现场表达失真。

### 可用性、可信度与隐私

127. 作为汇报用户，我想查看评论依据和置信度，以便自行判断是否采纳。
128. 作为汇报用户，我想让解析异常和低置信度内容被明确标记，以便不会把不确定判断当成事实。
129. 作为汇报用户，我想让项目、文件和模型上下文彼此隔离，以便材料不会串到其他用户或项目。
130. 作为汇报用户，我想让文件传输和存储加密，以便降低敏感材料泄露风险。
131. 作为汇报用户，我想使用短期有效的下载链接，以便文件不会通过永久公开地址暴露。
132. 作为汇报用户，我想删除项目和源文件，以便控制数据生命周期。
133. 作为汇报用户，我想确认自己的 PPT 不会默认用于模型训练，以便保护商业和研究材料。
134. 作为键盘用户，我想通过键盘浏览评论、展开详情、定位页面和处理 Diff，以便不依赖鼠标完成主要流程。
135. 作为屏幕阅读器用户，我想获得头像身份、评论状态、选区状态和 Diff 变化的文本说明，以便理解界面内容。
136. 作为网络不稳定的用户，我想在可恢复错误后重试，而不是丢失已完成的分析和编辑草稿，以便继续工作。

## Implementation Decisions

### 产品与范围决策

- 首版只正式接收 PPTX，单文件最多 60 页。PDF 只读评审不属于本 spec。
- 首版从产品模型层面支持工作汇报、学生答辩、学术报告、创业路演和公开演讲，不把非职场场景推迟为未来架构问题。
- 职场资源申请是默认 Demo 与初期样本之一，不是唯一允许场景。
- 汇报主题、场景、真实听众和预计时长为必填；汇报目标与期望听众回应为可选。
- 汇报目标和期望听众回应留空时，AI 生成建议值；字段必须保存来源状态，至少区分 `ai_suggested` 与 `user_confirmed`。
- 默认邀请 3 位点评人，最多 4 位。第 4 位仅在存在独立高风险维度且有页面或全局结构证据时加入。
- 首屏默认展示 5 个最高优先级问题主题，其余问题折叠。
- 不实现独立文字问答预演。潜在问题作为评论详情或逐页讲稿的一部分静态生成。

### 领域模型

- `Project` 表示一次汇报预演，保存场景、听众、目标、目标来源、期望回应、回应来源、时长和状态。
- `SourceFile` 表示用户上传的不可变原始 PPTX，保存文件哈希、页数、大小、解析状态和对象存储引用。
- `DeckVersion` 表示一次可下载的演示文稿快照，保存父版本、修改摘要、生成状态和布局警告。
- `Slide` 使用稳定页面 ID，不以会随排序变化的页码作为唯一标识；页码只是当前版本的展示顺序。
- `SlideElement` 使用稳定元素 ID，保存元素类型、边界、文本、样式和是否允许受控编辑。
- `DeckContext` 保存当前版本的共享事实底稿、叙事结构、观点、证据链接、缺失、矛盾和页面作用。
- `Reviewer` 保存角色身份、职责、评价量表、提示词版本和当前项目启用状态。
- `ReviewIssue` 保存问题作用域、页面与元素定位、严重度、类别、依据、建议、置信度和版本状态。
- `IssueCluster` 保存重复问题主题以及其包含的角色评论，统一维护处理状态。
- `ReviewComment` 保存 50—100 字评论正文、展开详情、角色、关联问题、关联页面、展示顺序和生成状态。
- `RewriteProposal` 保存选区、原文、建议文本、上下文版本、生成状态和用户决策，不直接表示已提交版本。
- `ChangeSet` 保存用户本轮接受的改写、手工编辑、排序和隐藏操作。
- `Script` 绑定一个 DeckVersion，保存逐页目的、讲述顺序、讲稿、衔接语、时长和潜在问题。

### 主要测试 seam

- 将“项目工作流服务/API”设为最高层、主要的行为测试 seam。测试通过该 seam 创建项目、上传文件、推进分析、查询评论、请求局部改写、接受或拒绝建议、提交版本、生成讲稿和下载文件。
- PPTX 处理器、页面渲染器、模型网关、对象存储和任务队列是工作流服务背后的适配器。行为测试使用可控替身，不通过多个内部模块 seam 重复断言同一流程。
- 对真实 PPTX 兼容性另设少量契约测试和样本测试，验证第三方库或外部程序的真实行为；这类测试不取代主工作流行为测试。
- UI 只对关键交互做组件或端到端测试：评论流展开、文字选择、动画状态、Diff、接受/不接受、提交版本及错误恢复。测试断言用户可见行为，不断言内部组件层级。

### 系统模块边界

- 项目与背景模块负责项目生命周期、场景、听众、目标和来源状态。
- PPTX 文档模块负责解析、稳定 ID、受控修改、排序、隐藏、恢复和导出。
- 渲染与布局模块负责页面图片、字体替换信息、溢出、遮挡和明显布局变化检测。
- 分析编排模块负责长任务状态机、逐页视觉理解、DeckContext、增量失效和重试。
- 点评模块负责风险维度、角色路由、角色评价量表、ReviewIssue、ReviewComment 和问题聚类。
- 编辑建议模块负责文字选区、上下页上下文组装、RewriteProposal、Diff 和用户决策。
- 版本模块负责 ChangeSet、不可变版本、历史查看、恢复和下载。
- 讲稿模块负责绑定当前版本生成逐页讲稿、衔接、时间分配和潜在问题。
- 观测与评估模块负责任务耗时、失败阶段、评论处理、改写采纳和质量反馈事件。

### 上传与处理契约

- 上传前校验扩展名、媒体类型、文件头、文件大小和页数；页数超过 60 时拒绝创建分析任务。
- 原文件不可被后续操作覆盖。所有修改从某个 SourceFile 或 DeckVersion 派生。
- 文件处理是异步任务，阶段至少包括：上传完成、解析、逐页渲染、视觉理解、全局分析、路由、评论生成、完成和失败。
- 每个阶段提供可查询状态和可理解的失败原因。单页失败记录页码并允许其余页面继续。
- 解析优先使用 PPTX 原生文字和元素数据；视觉模型不重复 OCR 已可靠取得的文字。
- 60 页任务采用分页/分批处理，但用户看到统一进度；批次划分属于内部实现，不应影响最终页面顺序和跨页分析。
- 页面渲染与视觉结果按页面内容哈希和渲染环境版本缓存。页面未变化时可复用结果。

### 场景与 AI 目标建议

- 所有场景使用统一 Presentation Context Schema，场景差异通过评价维度、角色权重和讲稿风格扩展。
- AI 目标建议必须基于已解析 PPT 内容、用户选择的场景和真实听众，不只根据文件名生成。
- AI 建议在用户确认前不可伪装为用户输入；界面、API 和存储均保留来源状态。
- 用户修改或确认目标后，更新 DeckContext，并使依赖目标的角色路由、问题严重度和讲稿失效；文件解析和未变化页面视觉理解保持有效。
- 如果 AI 无法可靠推断目标，返回需要补充的说明而不是空洞通用文本。

### 点评人与路由

- 第一批角色为 Jack、Emma、Ryan、Olivia、Mia、Leo 和 Sophie，各自拥有独立评价量表。
- 路由模型输出每个风险维度的分数、选择角色、理由、页码和置信度。
- 路由角色数必须为 3 或 4；除非项目可分析页面不足或分析失败，不返回少于 3 位。
- 角色共享 DeckContext 和当前版本，不各自重新解释整套文件。
- 角色只能在职责范围内生成评论，跨职责问题交由更匹配角色或聚类层处理。
- 评论不得虚构真实机构、头衔、职位或经历，所有角色必须显示“AI 模拟点评人”。

### 评论生成与展示

- 评论正文在去除首尾空白后为 50—100 个显示字符。中英文字符、数字和标点均按一个显示字符计；连续空白在展示前折叠。
- 评论正文只包含一个主要判断。依据、影响解释、建议动作、置信度和潜在问题使用结构化详情字段保存。
- 评论正文不足 50 或超过 100 个显示字符时，生成结果不直接发布；由结构化修复或重试流程调整。
- 收起态显示头像、姓名、小标题、正文、关联页码、严重度和生成状态。
- 展开态在同一右侧面板内显示详细依据，不跳转到独立问答页面。
- 评论头像和新评论使用轻量出现动画。动画不得阻断点击、滚动或页面渲染，并支持 `prefers-reduced-motion` 等效降级。
- 右侧面板在桌面宽屏与页面预览并列；窄屏使用覆盖式抽屉，但保持用户的当前页面和滚动位置。
- 跨角色评论只在根因相同、页面相同或重叠、且可由同一操作解决时聚类。聚类保留每条原始评论和不同影响解释。

### 页面文字选择

- 可编辑文字必须来自可稳定映射的 PPTX 文本元素。系统不能可靠映射的渲染文字只可查看，不提供选择改写。
- 选区由 DeckVersion、Slide 稳定 ID、SlideElement 稳定 ID 和文本起止偏移共同标识。
- 发起改写前校验选区原文仍与当前编辑草稿一致，防止并发或连续编辑导致错位。
- 首版只支持单个文本元素内的连续选区，不支持跨文本框、跨页或非连续多选。
- 选区完成后显示高亮和轻量呼吸/聚焦动画，并在附近显示 AI 修改浮动工具条。
- AI 生成期间锁定该选区的重复请求，但不应锁死页面其他只读浏览行为。
- 用户取消选择、切换页面或选区失效时，关闭浮动工具条并清理临时状态。
- 键盘与屏幕阅读器应获得等效的选区状态、操作入口和生成状态反馈。

### AI 局部改写契约

- 改写请求包含当前 DeckVersion、选区原文、所在元素全文、当前页文本与页面作用、上一页文本与页面作用、下一页文本与页面作用、相关视觉摘要、场景、听众、目标和期望回应。
- 首页省略上一页上下文，末页省略下一页上下文；缺少相邻页不是错误。
- 模型默认只返回选区替换文本、改写理由、事实保持检查和置信度，不返回整页重写。
- 模型不得无授权改变数字、专有名词、事实关系和用户观点。检测到建议改变受保护信息时，结果标记风险并不默认可接受。
- RewriteProposal 的初始状态为待决策。用户“接受修改”后进入当前 ChangeSet；用户“不接受”后状态为已拒绝，原文保持不变。
- 接受 RewriteProposal 不立即创建 DeckVersion。只有用户提交本轮编辑时才生成新版本。
- Diff 至少区分原文、删除、增加和替换，并可以在不依赖颜色的情况下理解。
- 生成失败不应破坏选区原文或其他编辑草稿，用户可以重试或取消。

### 受控编辑与版本

- 受控编辑仅包含现有标题、正文、备注、页面顺序和隐藏状态。
- 不支持新增、移动、缩放页面元素，也不修改图表数据、图片、SmartArt、动画、转场、视频和母版。
- ChangeSet 提交必须基于明确的父 DeckVersion。若当前版本已变化，系统要求用户刷新或解决冲突，不静默覆盖。
- 新版本生成后重新解析已改文字、渲染受影响页面、检测布局、更新页面摘要、轻量更新全局叙事并复查关联问题。
- 页面顺序改变时重新计算全局叙事、页间衔接、讲稿失效和时间分配，不重跑未变化页面视觉理解。
- 页面隐藏或恢复时保留原页面内容和稳定 ID，只更新演示路径、叙事、时长和相关问题状态。
- 版本恢复通过基于历史快照创建新版本完成，保持历史链不可变。
- 布局警告不阻止下载，但下载前必须展示受影响页码和建议在 PowerPoint 中复查的说明。

### 逐页讲稿

- 讲稿只能从明确 DeckVersion 生成；版本变化后旧讲稿标记为非当前版本。
- 每页输出页面目的、核心信息、推荐顺序、口语参考、前后衔接、建议时长、可省略内容和潜在问题。
- 所有页面建议时长之和应与用户预计时长一致或给出可解释差异；超时时必须提供压缩策略。
- 讲稿可选择自然口语、正式汇报、学生答辩或学术报告风格，场景可以提供默认风格但用户可覆盖。
- 讲稿不得引入 DeckContext 中不存在的新事实。推测性表达必须标记为建议而非事实。
- 潜在问题是静态准备内容，不创建文字问答会话或追问状态机。

### API 行为边界

- 项目 API 支持创建、读取列表、读取详情、更新背景、确认 AI 建议和删除。
- 文件 API 支持初始化上传、完成上传、读取处理状态和获取受控下载地址。
- 分析 API 支持启动分析、查询阶段与进度、重试可恢复失败和读取 DeckContext 摘要。
- 点评 API 支持读取路由结果、启用或取消角色、读取评论流、展开评论详情、筛选问题、处理问题和读取聚类。
- 编辑 API 支持创建文字选区改写请求、读取 RewriteProposal、接受或拒绝、更新 ChangeSet、提交版本和撤销草稿。
- 页面结构 API 支持重新排序、隐藏和恢复，并把操作写入 ChangeSet。
- 版本 API 支持列表、详情、恢复、布局警告和下载。
- 讲稿 API 支持按版本和风格生成、查询状态和读取逐页结果。
- 长任务 API 返回任务标识与状态，不要求单次同步请求等待模型或文件处理完成。

### 数据安全与观测

- 对象存储文件和数据库敏感字段在存储与传输时加密，下载地址短期有效。
- 模型调用按项目和版本构建上下文，禁止跨项目复用原始内容。
- 日志不得默认记录完整 PPT 文本、完整模型输入或下载地址。
- 用户删除项目时进入明确的数据删除流程；源文件、派生版本、渲染图和模型缓存遵循同一生命周期策略。
- 用户材料默认不用于模型训练，除非获得单独、明确且可撤回的授权。
- 关键观测事件覆盖上传、分析阶段、角色路由、评论曝光与展开、问题处理、文字选择、改写生成、接受/拒绝、版本提交、布局警告、讲稿生成和下载。
- 质量评估事件保存问题类别、严重度和处理结果，但不把完整敏感正文写入分析事件。

## Testing Decisions

### 总体测试策略

- 好测试只验证用户或外部调用者能观察到的行为、状态变化、输出文件和错误，不断言内部函数调用次数、私有类结构或具体队列实现。
- 优先通过单一的项目工作流服务/API seam 覆盖完整业务链路，避免为分析、评论、编辑和版本分别建立重复的高层测试夹具。
- 外部 AI、对象存储和队列在主行为测试中使用确定性替身，使测试可重复并可精确构造异常。
- PPTX 解析、修改和渲染不能只使用 mock；必须用真实文件样本做契约与回归测试。
- 当前仓库没有既有代码或测试先例。实现开始后，首批测试结构将成为该项目的 prior art，应在后续模块沿用同一工作流夹具和样本库。

### 工作流行为测试

- 测试有效 1 页、15 页、30 页和 60 页 PPTX 可以进入分析；61 页被拒绝且返回明确错误。
- 测试非 PPTX、损坏文件、加密文件和不支持内容返回用户可理解的错误，不创建悬空分析任务。
- 测试上传、解析、视觉理解、全局分析、路由、评论生成和完成阶段按外部可查询状态推进。
- 测试单页渲染失败时其他页面继续，最终结果包含异常页标记。
- 测试失败阶段可以重试且不会重复创建源文件或丢失已完成页面结果。
- 测试汇报目标和期望回应为空时分析仍可启动，并产生带 `ai_suggested` 来源的建议。
- 测试用户修改或确认建议后来源变为 `user_confirmed`，依赖结果失效而页面解析缓存保持可复用。
- 测试五类场景均可通过同一工作流创建并产生场景相关的分析结果。

### 角色与评论测试

- 测试普通项目默认产生 3 位角色，高风险且独立的第 4 维度产生第 4 位角色，不超过 4 位。
- 测试每个路由结果包含理由、关联页面和置信度。
- 测试取消角色后不再生成新的角色评论，重新邀请后读取当前版本。
- 测试角色身份始终包含 AI 模拟标识，不出现未配置的真实机构或履历。
- 测试评论正文经规范化后保持 50—100 个显示字符，详情字段不计入正文。
- 测试超长或过短模型输出在发布前被修复或拒绝，不直接进入评论流。
- 测试首屏最多 5 个高优先级主题，其余可通过“更多问题”读取。
- 测试同根因评论正确聚类，不同根因或不同修改动作的问题不被错误合并。
- 测试展开评论返回依据、影响、建议动作和页面定位，同时保持当前页面上下文。
- 测试筛选器按状态、严重度和角色返回正确集合。

### UI 交互测试

- 测试新评论出现时头像和状态有可观察反馈，动画结束后内容可点击且布局稳定。
- 测试启用减少动态效果时不运行非必要缩放/呼吸动画，但信息与操作保持完整。
- 测试评论收起、展开、再次收起不会丢失当前页面和滚动位置。
- 测试桌面布局中评论流与页面并列，窄屏切换为抽屉且能关闭返回。
- 测试评论到页面定位会选中正确稳定 Slide ID，而不是旧页码。
- 测试键盘可以聚焦评论、展开详情、跳转页面、处理建议和操作 Diff。
- 测试屏幕阅读器获得评论状态、加载状态、选区状态及 Diff 的非颜色说明。

### 文字选择与 AI 改写测试

- 测试用户可在单一可编辑文本元素中选择连续文本，选区偏移与原文一致。
- 测试跨元素选择、非文本内容和无法稳定映射的文字不提供 AI 修改。
- 测试选区出现高亮、动画和浮动工具条，取消或切页后临时状态清除。
- 测试改写请求包含当前页、存在的上一页和存在的下一页内容；首页与末页正常降级。
- 测试连续点击 AI 修改不会产生重复并发请求。
- 测试在请求期间原文和编辑草稿不被提前改变。
- 测试模型只返回选区替换文本；若返回整页内容，适配层拒绝或修复该结果。
- 测试数字、术语或事实被建议改变时出现风险标记。
- 测试 Diff 可以区分增加、删除和替换，并具有非颜色表达。
- 测试接受建议只更新 ChangeSet，不立即创建 DeckVersion。
- 测试“不接受”保持原文且不创建版本。
- 测试接受后用户可以继续手工编辑或撤销草稿。
- 测试生成失败和重试不会破坏原文、选区或其他草稿修改。
- 测试选区创建后文本被其他操作改变时，提交请求返回选区失效而非修改错误位置。

### PPTX 契约与样本测试

- 建立不少于 20 套真实 PPTX 样本，覆盖中文与英文字体、主题、母版、分组、表格、图表、SmartArt、图片、备注和隐藏页。
- 对每个样本验证页数、顺序、隐藏状态、文本、备注、元素边界和稳定 ID 的解析结果。
- 对受支持文本元素验证修改后可由 PowerPoint 正常打开，未编辑元素保持不变。
- 验证调整顺序、隐藏、恢复和版本恢复后的导出文件可打开且行为正确。
- 对相同未变化页面验证内容哈希和缓存命中；对文字改变页面验证缓存失效。
- 在目标部署环境验证中文字体替换和渲染差异，建立视觉基线或结构化容差。
- 验证溢出、遮挡和明显布局变化检测能识别预置异常，并控制误报。
- 使用 15 页、30 页和 60 页样本记录处理时间、内存和模型成本，验证容量方案。

### 增量复查与版本测试

- 测试单页文字提交仅重渲染该页并复查关联问题，其他页视觉结果保持有效。
- 测试页面排序使全局叙事、衔接、时长和旧讲稿失效，但不重新理解未变页面图片。
- 测试隐藏和恢复只改变演示路径及相关全局结果，页面稳定 ID 和内容保持不变。
- 测试每次提交生成新版本，父版本不可变且仍可查看。
- 测试从历史版本恢复会创建新的当前版本，不重写历史记录。
- 测试并发基线冲突被明确拒绝，不静默覆盖较新的版本。
- 测试问题状态随版本变为待复查、已解决、仍然存在或已失效。
- 测试布局警告包含页码并在下载前展示，但用户确认后仍可下载。

### 讲稿测试

- 测试讲稿绑定请求时的 DeckVersion，版本变化后旧讲稿标记为过期。
- 测试每页结果包含目的、核心信息、顺序、参考稿、衔接、时长、可省略内容和潜在问题。
- 测试总建议时长与用户目标一致；不可一致时必须带解释和压缩建议。
- 测试四种讲稿风格产生风格差异但不改变事实。
- 测试模型引入未知事实时结果被标记或修复。
- 测试讲稿生成不会创建文字问答会话或追问状态。

### 安全、恢复与观测测试

- 测试用户无法读取其他用户的项目、文件、版本、评论或下载地址。
- 测试下载地址过期后失效且不可枚举。
- 测试默认日志和埋点不包含完整 PPT 正文、模型输入或长期下载凭证。
- 测试删除项目触发源文件、版本、渲染图和缓存的既定生命周期流程。
- 测试任务幂等性，重复回调或重试不会产生重复版本与重复评论。
- 测试关键事件包含 project、version、issue、reviewer 和 slide 等必要标识，但不包含敏感正文。
- 测试网络中断或页面刷新后可以恢复分析进度和未提交编辑草稿。

## Out of Scope

- PDF 上传和只读评审。
- 从零生成整套 PPT。
- 完整在线 PowerPoint 编辑器。
- 新建、移动、缩放或自由排列页面元素。
- 自动修改图片、图表数据、SmartArt、动画、转场、视频和母版。
- 跨文本框、跨页面或非连续多选文字改写。
- 独立文字问答预演、AI 追问会话和文字回答评分。
- 语音、视频、表情、姿态和肢体表达分析。
- 自动验证外部数据、引用和研究结论的真实性。
- 用户自定义点评人及自定义角色提示词。
- 真人专家、真人在线点评和实时多人协作。
- 企业知识库、团队空间、组织权限、订阅和计费。
- 移动端完整 PPT 编辑体验；窄屏只保证评论浏览和基础导航的可用性。
- 自动修复所有布局警告；首版只检测并提示用户在 PowerPoint 中检查。
- 将用户 PPT 用于模型训练，除非未来有独立且明确的授权机制。

## Further Notes

### 尚待实现前确认

- PPTX 处理器最终选择 Aspose.Slides、Open XML SDK 组合或其他方案，需要由真实样本验证决定。
- 页面渲染使用 Aspose.Slides 还是 LibreOffice headless，需要比较中文字体、复杂元素保真度、性能和授权成本。
- 文件大小上限尚未确定；页数上限已确定为 60 页。
- 7 位角色的最终姓名可以沿用当前方案，但头像视觉、语言风格和具体评价量表仍需设计评审。
- 默认 Top 5、3—4 位角色和 50—100 字评论已作为本 spec 的行为要求。
- “50—100 字”在本 spec 中落地为规范化后的显示字符数；若产品希望按中文词语或模型 token 计算，应在开发前改写该规则。
- 评论流头像和选区动画需要设计稿确定时长、曲线和视觉强度，但必须满足不阻塞操作及减少动态效果降级。
- 目标生成发生在轻量解析后还是完整分析阶段，可由性能验证决定；外部行为必须保证留空不阻塞、建议可编辑且来源可见。
- 首个 Demo 继续使用职场资源申请样本，同时测试集必须包含学生答辩、学术报告、创业路演和公开演讲样本。

### 性能与质量基线

- PRD 中提出 15 页 PPT 从上传完成到首批可用问题的中位数目标不超过 5 分钟。60 页性能目标需通过 M0 测试后补充。
- 评论质量优先于数量：有效问题发现率、高优先级采纳率和无效建议率比生成评论条数更重要。
- 应将“用户发现至少一个此前未意识到且值得修改的问题”作为首轮产品价值验证重点。

### issue tracker 发布说明

当前目录不是 Git 仓库，也没有提供项目 issue tracker、项目标识或 triage 标签词汇，因此本轮只生成本地 spec，未执行发布和 `ready-for-agent` 标记。若需要完全执行 `to-spec` 的发布步骤，请先配置对应 issue tracker，或运行该技能建议的 `/setup-matt-pocock-skills` 完成项目设置。
